{% set pages = craft.entries.section('pages').descendantOf(entry).all() %}


<div class="flex flex-col rounded-lg border border-gray-300 overflow-hidden">
            <h3 class="block text-xl font-semibold text-gray-900 border-b border-gray-300 px-4 py-3 uppercase">
              {{ entry.parent.title }}
            </h3>
            <div class="flex-1 bg-white p-6 flex flex-col justify-between">
              <div class="flex-1">
              
                <ul class="space-y-4">


                
                {% nav page in pages %}

                  <li class="text-base truncate">
                    <a href="{{page.url}}" class="font-medium text-gray-900 hover:text-gray-700">
                      {{ page.title }}
                    </a>
                  </li>

                {% endnav %}
                
              </ul>
              </div>
            </div>
          </div>


